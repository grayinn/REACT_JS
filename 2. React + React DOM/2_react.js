// ----------------------- REACT, REACT DOM ------------------------------

/**
 *      1. document.createElements()
 *      2. document.body.appendChild() (thêm con vào)
 *      3. console.dir()
 *      4. innerText, id, className, style
 */

// ----------------------- React.createElements() -------------------------

/**
 *      1. React.createElement() -> React element (thành phần nhỏ nhất khi làm việc với React)
 *      2. So sánh với document.createElement() -> DOM element
 *      3. Thay đổi: id, className, style, ...
 *      4. Thêm: text, HTML
 */
// Mục đích: render ra giao diện người dùng

//  So sánh với document.createElement() -> DOM element

